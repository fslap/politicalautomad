lib/ directory for highly self-dependent components only.

Example: if a component needs its own copy of leaflet.js (and not already in theme base), put here exactly as done in v2-xxx branches of politicalpartysite.

Do not copy full theme base CSS/JS from other roots.