# LeafletMap Component

Interactive map for party locations, events, offices.

**Special rule applied**: Leaflet assets (JS/CSS) should only be included in this component if they are **not already merged** into the main theme base files.

Full pattern followed (array data, self-contained templates, full header).