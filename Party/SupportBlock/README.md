# SupportBlock Component

Calls-to-action for donations, membership, volunteering.

Same strict rules applied: array data, mustache init in base, self-contained templates (no imports), specific assets only, full header.

This is the pattern to repeat for every remaining component from the v2- branches.