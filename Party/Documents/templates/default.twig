{# Self-contained documents list #}
<div class="documents-block">
  <h3>Documents &amp; Downloads</h3>
  <ul>
    {% for doc in documents %}
    <li><a href="{{ doc.url }}" target="_blank">{{ doc.title }}</a> <span class="meta">{{ doc.type }}</span></li>
    {% endfor %}
  </ul>
</div>