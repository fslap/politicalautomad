{{^mobile}}
<div class="wp-block-group {{classes}}">
    {{#layers}}
        <div class="{{position}}">
            {{#svgs}}
                <img src="{{file}}" class="absolute {{position}} svg {{animation}}" alt="Water animation layer">
            {{/svgs}}
        </div>
    {{/layers}}
</div>
{{/mobile}}
